import { View, Text } from 'react-native'
import React from 'react'
import PrimaryButton from '../../components/shared/PrimaryButton'

const Tab1Screen = () => {
  return (
    <View>
      <PrimaryButton label='Probando navegacion' onPress={()=>{}}/>
    </View>
  )
}

export default Tab1Screen